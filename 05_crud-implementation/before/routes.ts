import { Hono } from 'hono'
import { PrismaClient } from '@prisma/client'

const app = new Hono()
const prisma = new PrismaClient()

// ---------------------------------------------------------------------------
// 記事一覧取得
// TODO: クエリパラメータのバリデーション（cursor, take, status, tag）
// TODO: カーソルベースのページネーション
// TODO: status / tag によるフィルタリング
// TODO: 一覧では content を除外する
// TODO: エラーレスポンスの統一フォーマット
// ---------------------------------------------------------------------------
app.get('/posts', async (c) => {
  // TODO: 実装する
  return c.json({ data: [], nextCursor: null })
})

// ---------------------------------------------------------------------------
// 記事詳細取得
// TODO: author, tags をネストして返す
// TODO: 存在しない場合は 404
// ---------------------------------------------------------------------------
app.get('/posts/:id', async (c) => {
  // TODO: 実装する
  return c.json({})
})

// ---------------------------------------------------------------------------
// 記事作成
// TODO: リクエストボディの Zod バリデーション
// TODO: title からの slug 自動生成（ケバブケース変換 + 重複時サフィックス付与）
// TODO: authorId の存在確認
// TODO: tags の connectOrCreate 処理
// TODO: 成功時 201
// ---------------------------------------------------------------------------
app.post('/posts', async (c) => {
  // TODO: 実装する
  return c.json({}, 201)
})

// ---------------------------------------------------------------------------
// 記事更新
// TODO: リクエストボディの Zod バリデーション（全フィールド任意）
// TODO: 記事の存在確認
// TODO: title 変更時の slug 再生成
// TODO: tags 差し替え（既存を全解除 → 新規接続）
// ---------------------------------------------------------------------------
app.put('/posts/:id', async (c) => {
  // TODO: 実装する
  return c.json({})
})

// ---------------------------------------------------------------------------
// 記事削除
// TODO: 記事の存在確認
// TODO: 削除処理
// TODO: 204 レスポンス（ボディなし）
// ---------------------------------------------------------------------------
app.delete('/posts/:id', async (c) => {
  // TODO: 実装する
  return c.body(null, 204)
})

export default app
