# レガシーコード調査メモ - 2026/03/10（月）

担当: 田中（入社3ヶ月目）
対象: sales-pipeline/ 以下のPythonコード
前任者: 退職済み、引き継ぎ資料なし

---

9:15 全体のファイル構成を確認した。
main.py, proc.py, calc.py, viz.py, util.py, cfg.py の6ファイル。
READMEもdocstringもない。コメントゼロ。セミコロンで1行に複数文を詰め込んでいるのでエディタの折り返しがないと追えない。

9:30 cfg.py を開く。
最初の行で `_=os.path;B=_.dirname;J=_.join` とあり、osモジュールのメソッドを1文字に束ねている。これだけでかなり読む気が削がれる。
D は辞書。chr(97)からchr(101)、つまり'a'から'e'のキーに日本語カラム名を割り当てている。売上日、商品名、カテゴリ、金額、個数。
F, F2 はリスト内包でファイルパスを生成。sales.csv と sales_prev.csv。
T=0.05, W=7, Q=[3,6,9,12] の3つがカンマ区切りで1行に。
末尾に _R と _V がある。_R は "H","M","L" をキーにした辞書でワンホットベクトルっぽい値。_V はそれを返すlambda。
→ _Rと_Vはどこで使われている？ grepしたがcfg.py以外で参照がない。何の優先度分類だろう。死んでいるコードかもしれない。

10:00 util.py を読む。
chk はlambda。isinstance と datetime.strptime を1行に詰めている。正規表現で先にフォーマットチェックしてからstrptimeに渡す二重チェック。日付バリデーションだろう。
fix も1行の関数。通貨記号の除去と数値変換。re.subの中に `¥￥,$\s,` が並んでいて、カンマが2回入っている気がする。バグ？ それとも意図的？
fmt はlambda。1e6以上なら"M"、1e3以上なら"K"。数値フォーマット。
→ ユーティリティとしては読めた。chkの1行lambdaは初見殺しだが、分解すれば理解できる。

10:30 proc.py に進む。
fn1(df) -- 最初の行が三項演算子の入れ子。statusカラムの存在チェック、isinでcancelled/returnedを除外、なければそのまま。
次にdrop(columns=["status"], errors="ignore")。なるほど、filterした後にstatus列自体を消す。
returnの行が長い。金額>0、かつ売上日と金額がnotna、の両方を満たす行だけ残してdrop_duplicatesしてreset_index。
→ データのクリーニング処理で間違いなさそう。

fn2(df, d=None) -- c,e,a にD["d"],D["e"],D["a"]を割り当て。日付をdatetime化。d引数があれば日付フィルタ。
groupbyのaggが辞書内包表記。`{k:(c if k!="cnt" else e,v) for k,v in[("total","sum"),...]}` ――これはきつい。
展開してみると、total=金額のsum、cnt=個数のsum、avg=金額のmean、mx=max、mn=min、n=count。
→ 日付ごとの日次集計。ここまでは合っているはず。

fn3(df, c=None) -- pivot_table。カテゴリ別の金額・個数集計。
pctとrankの代入が1行にカンマで詰められている。
→ カテゴリ別の売上分析。構成比と順位を付与。

11:15 calc.py に着手。覚悟を決める。

x(a, b) -- 2つの系列をリスト内包で一括Series化。長さが違えばslice。
gとdの代入もカンマ区切りで1行。g = (cur-prev)/prev*100、d = cur-prev。
→ 変化率と差分。前年同期比の計算だと思う。

y(df) -- next()で"total"か"sum"のどちらかを探す。なければdf.columns[1]をフォールバック。
rolling(window=W)でmeanとstdを計算。リスト内包でagg("mean")とagg("std")を同時に作っている。
upper = ma + 2*std、lower = ma - 2*std。anomaly = 範囲外ならTrue。
→ 移動平均ベースの異常検知。ボリンジャーバンドの構造に近い。
  でも待て。売上データにボリンジャーバンドを使う意味があるのか？ 統計的管理図の発想？
  確信度: 60%。移動平均と2σバンドであること自体は間違いないが、ビジネス上の意図は読み取れない。

z(df, t="total") -- ここが一番厳しい。
df["m"]の代入が三項演算子3段ネスト。dateカラムがあれば月を抽出、なければmカラムを見る、それもなければ連番を12で割った余り。
s = 月ごとの平均。gs = 全体平均。si = s / gs。
df["si"]とdf["adj"]をカンマ区切りで1行代入。adjは si != 0 なら t/si、そうでなければ t そのまま。
trendは adjの3日移動平均。
→ 季節調整？ 月別の季節指数を出して、実績を割ることで季節要因を除去？
  経済統計の手法だとは思うが、このパイプラインの中でなぜ季節調整が必要なのか分からない。
  全体の中でのz()の位置づけが見えない。

12:00 昼休憩。calc.pyの3関数のうち、確信があるのはx()だけ。

13:00 viz.py を見る。
mk(df, p) -- リスト内包で__setitem__を呼んでいる。副作用のためだけにリスト内包を使うスタイル。
fmtで数値を整形してCSVに書き出す。
→ CSV出力。リスト内包の副作用利用は悪趣味だが、処理内容は単純。

rpt(df, df2=None) -- リスト連結でテキストレポートを組み立てている。
三項演算子でtotalカラムの有無を判定。df2がNoneでなければYoYセクションを追加。
→ テキストレポート生成。出力側なので比較的読みやすい。

13:30 main.py を読む。
run() が全体を制御。中身は3行。
1行目: `df=fn1(pd.read_csv(F,encoding=E));res=z(y(fn2(df)));cat=fn3(df)`
  チェーンが深い。fn2の結果にy()をかけ、その結果にz()をかける。
2行目: try-exceptで前年データ。fn1→fn2→x()。失敗したらcmp=None。
3行目: リスト内包でmk()を2回呼び、rpt()を返す。
→ パイプラインの流れは追える。順序は: CSV読込 → fn1 → fn2 → y → z → fn3 → 前年比較 → CSV出力 → レポート

14:00 疑問メモ:
- T = 0.05 はどこからも使われていない。閾値？ 税率？ デッドコード？
- Q = [3, 6, 9, 12] も使われていない。四半期処理が未実装のまま放置？
- _R, _V は何？ 優先度のワンホットエンコーディング？ でも参照がない
- fn2のd引数、fn3のc引数はmain.pyでどちらもNoneのまま。フィルタ機能があるのに使っていない
- cfg.pyのS（逆引き辞書）もどこからも呼ばれていない
- util.pyのchk()とfix()もパイプラインで未使用。fmtだけ使われている

14:30 fix()のre.subの中身を改めて見る。
`r"[¥￥,$\s,]"` -- 全角の¥、半角の￥...いや逆か。¥が半角で￥が全角？ $はドル記号。\sはスペース。カンマが最後にもう1個ある。
カンマが文字クラスの中に2回入っている。片方が不要だと思うが、動作に影響はないはず。
→ バグではないが、雑なコード。

15:00 z()をもう一度読む。
si = s / gs -- 各月の平均を全体平均で割る。これは確かに季節指数。
adj = t / si -- 実績を季節指数で割る。これが季節調整値。
trend = adjの3期移動平均。
→ やはり季節調整。月次の季節変動を除去して基調トレンドを取り出す処理。

ただし、z()の第2引数tは列名で、main.pyからは引数なし（デフォルト"total"）で呼ばれている。
y()で追加したma, std, upper, lowerの列はz()に渡されるが、z()は"total"しか見ない。
→ y()の出力にz()をかける意味があるのか？ 互いに独立した分析を同じDataFrameに追加しているだけ？
  だとしたらy()とz()の順序は関係ない。別々に呼んでも同じ結果になるはず。
  ...と思ったが、y()がstd列を追加するとz()のdf.columns[1]フォールバックの挙動が変わる。
  いや、"total"が存在するからフォールバックは踏まない。多分大丈夫。多分。

15:30 分かったこと・分からないこと:

分かった（ほぼ確信あり）:
- cfg.py: パス定義とカラム名マッピング（chr()でaからeを生成）
- util.fmt: 数値フォーマット（M/K/カンマ区切り）
- proc.fn1: フィルタリング（キャンセル除外、金額正、欠損除去、重複排除）
- proc.fn2: 日次集計（groupby + agg）
- proc.fn3: カテゴリ別集計（pivot_table + 構成比・順位）
- calc.x: 前年比較（成長率%と差分）
- viz.mk: CSV出力（fmt適用）
- viz.rpt: テキストレポート出力

分かったが自信がない:
- calc.y: 7日移動平均 + 2σバンドでの異常検知。ロジックは読めたが、なぜ売上データにこれを適用するのかの動機が見えない
- calc.z: 月別季節指数による季節調整。手法は分かるが、日次データに月単位の季節調整をかける粒度の妥当性が判断できない

分からない・諦めた:
- _R, _V の意図。使われていないので無視するしかない
- T, Q, S, chk, fix が未使用な理由。過去の仕様変更で取り残された残骸？
- パイプライン全体のビジネス要件。「日次で売上を集計してトレンドと異常を分析する」はおそらく正しいが、誰がどんな目的で見るレポートなのかは不明

16:00 main.pyのtry-exceptがException全キャッチなのが気になる。
前年ファイルがなければcmp=None。それはいい。
だが、fn1やfn2のバグもここで握りつぶされる。前年データ処理にバグがあっても静かにNoneになる。
→ 運用中に問題が起きたら原因特定が困難。

16:30 チームリーダーへの報告用にまとめ:
- パイプラインの骨格は追えた。CSV → クリーニング → 日次集計 → 統計 → 出力
- calc.pyの統計処理は手法レベルでは理解したが、ビジネス要件との結びつきが不明
- デッドコードが多い。6個以上の未使用変数・関数がある
- コード全体が1行詰め込みスタイルで、意図的に読みにくくしたのかと疑うレベル

ただ正直に言うと、「たぶんこういう処理です」の域を出ない。
前任者のコードに対する自分の理解が正しいかどうかを検証する手段がない。

17:00 退勤。丸1日使って骨格は追えた。細部は推測の積み重ね。
